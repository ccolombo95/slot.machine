{
  "name": "vite.javascript",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch data/db.json --port 8080"
  },
  "devDependencies": {
    "sass": "^1.77.8",
    "vite": "^5.2.0"
  }
}
